
### Leihs Lending disabled ###########################################################
# publish_lending is false: exclude /lending from proxying so it doesn't fall through
# to the central main.conf route (which is unconditional, needed for Cider-CI).

ProxyPass /lending !

# vim: ft=apache
